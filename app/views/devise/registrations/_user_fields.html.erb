  <div>ФИО<br />
  <%= f.text_field :fio, :autofocus => true %></div>

  <div>Дата рождения<br />
  <%= f.date_select :dr, use_two_digit_numbers: true, start_year: 1900 %></div>

  <div>Роль<br />
    <%= f.collection_select(:role_id, Role.all, :id, :name) %></div>